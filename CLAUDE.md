# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

A personal, password-gated **trip-planning dashboard** for a working-holiday year in Tokyo: an editable calendar, booking-deadline notifications, a dependency-aware checklist, and a researched dataset. Vanilla HTML/CSS/JS as **ES modules** — no build step, no frameworks, no new CDNs. Served by GitHub Pages from `/docs`. All interactive state is browser-local (localStorage). Arrival anchor: **land NRT 2026-06-30**. Live at https://sizbei.github.io/japan-working-holiday/.

`specs/2026-06-15-trip-dashboard-design.md` is the design contract; `PLAN.md` has older directional notes.

## Run / verify

The page is ES modules + `fetch`es `data/tips.json`, so it MUST be served over HTTP (`file://` fails):

```bash
cd docs && python3 -m http.server 8000          # serve at http://localhost:8000  (password: lkjjapan)
node --test tests/lib.test.mjs                  # unit tests — run from the repo ROOT (zero deps)
python3 -m json.tool docs/data/tips.json > /dev/null   # validate the data
```

There is no bundler/linter. "Verify" = `node --test tests/lib.test.mjs` green + serve locally + page renders with no console errors.

**Browser-testing gotcha:** the app is a **hash router**, so navigating between `index.html#/a` and `index.html#/b` does NOT reload the document — it keeps running the JS/CSS from first load. To pick up code/CSS changes in an already-open tab, hard-reload, or load a URL with a unique cache-bust query (`index.html?v=N#/route`). Auth for testing: `localStorage['jwh-auth-v1'] = 'ok'` (literal `'ok'`, not `'true'`).

**Service worker gotcha:** `docs/sw.js` caches the app. It's network-first (fresh when online), but if a change doesn't show, hard-clear: in DevTools Application → unregister SW + clear cache, or bump `CACHE` in `sw.js`.

## Architecture

Still **data-driven from one file**: `docs/data/tips.json` is the single source of truth — adding content = editing JSON, never code. The JS reads it and renders. It's a **hash-router SPA** (11 nav routes + hidden deep-linked `deadlines|packing|phrases|grammar|study`). Modules under `docs/assets/`:

- `main.js` — boot: `gate → fetch tips.json → mount all features → initRouter → mountGestures → mountGuide → register SW`.
- `router.js` — the SPA hash router. Toggles `.is-active` on `#view-<route>`, animates view swaps (`motion.js` View Transitions), moves focus to the view heading, sets a **per-route `document.title`**, dispatches `jwh:route`. `ROUTES` is exported.
- `lib/` — **pure, unit-tested** logic (import-safe in Node): `dates.js` (countdown/window math), `notify.js` (`computeAlerts`), `ics.js` (`.ics` generate/parse + GCal URLs), `places.js` (saved-place CRUD), `geo.js` (neighbourhood centroids), `transit.js` (haversine/leg estimates), `dayplan.js` (itinerary store), `modal.js` (focus-trapped dialog promises), `study.js` (SRS engine: FSRS-lite scheduler, stages/gate, queue builder), plus `dom.js` (`$/$$/esc`) and `store.js` (versioned localStorage `KEYS`).
- `gate.js` — password overlay (`lkjjapan`). **Obfuscation, not security** — the password is in the JS and the repo is public; it only stops a shoulder-surfer. Remembered via `jwh-auth-v1`.
- `dashboard.js` — top bar (theme, live countdown, **top-right notifications bell + panel**) and home widgets. `buildItems()` assembles ONE alert stream (deadlines + book-by + future events + user checklist due dates + **R11 grammar `reviews due`** — ONE aggregate `study-reviews@<today>` item when `buildQueue` reports due>0, `kind:'review'`, `@date`-encoded so it never double-counts and re-surfaces tomorrow), `computeAlerts` ranks them. Dismiss ids encode `@date` so re-setting a date un-suppresses. The **文法ジム study widget** (`#wStudy`, `renderStudy()`) is the R11 habit front door: due-review count → ▶ Train button, the days-shown-up streak flame (+ monthly freeze count, at-risk nudge), the weekly-goal ring, per-level goal-gradient mastery rings — all derived by the pure `lib/study.js` selectors (`streakInfo`/`weeklyInfo`/`masteryStats`/`buildQueue`, `TODAY`-parametrised); landing on `#/dashboard` runs a full `refresh()` (study state, like budget/packing, doesn't dispatch `jwh:data-changed`). `dashboard-mytokyo.js` is the interests band.
- `calendar.js` — editable month/agenda calendar. Merges **baked** events (`tips.json.calendar`, read-only) with **user** events (`jwh-events-v1`, full CRUD). Tag-filtered `.ics` export, per-event GCal links, `.ics` import. Day cells/agenda rows use a real `<button>` trigger (the date / the title), NOT `role=button` containers. `eventsearch.js` is the search box on this page.
- `content.js` — domains (searchable + confidence filter), pillar grids, brew scratchpad, and the **dependency-aware checklist** (items lock until `requires[]` prereqs are checked; per-item due dates feed the bell; 100% fires a confetti/toast celebration). Explore cards are a disclosure pattern (a `.c-disclosure` button), with the Tabetai ★ as a sibling.
- `map.js` — **Leaflet 1.9.4 + markercluster** (lazy-loaded from unpkg with SRI, only on `#/map`, never precached). Pins from saved places + upcoming events + catalogue; OSM tiles, Nominatim geocoding. `placesModel()` is the shared point list (also used by the plan picker). Cold-start: `onMapShown()` polls for real canvas size before placing pins.
- `plan.js` — day-itinerary planner (`#/plan`); `rooms.js` — share-house finder (`#/rooms`).
- `gestures.js` — swipe between adjacent routes (touch), keyboard shortcuts (`1–8`, `[`/`]`, `?`), long-press quick-action menus (calendar days / explore cards / checklist rows).
- `guide.js` — the **⚙ Guide & Settings overlay** (a tutorial + theme/arcade/reduce-motion toggles). NOT a route.
- `grammar.js` + `lib/grammar.js` — JLPT grammar reference (`#/grammar`, hidden route linked from Survival Japanese): 353 points across `data/grammar-n[1-5].json` (per-level lazy fetch, SW stale-while-revalidate), token-level furigana/hover (own `.gtok` selector — NEVER `.jp`, which lang.js claims globally; cross-dismiss via `jwh:popover-open {owner}`), ✓/◆ tracking (`jwh-grammar-v1`). Data PRs run `scripts/validate-grammar.mjs` + a reading-accuracy critic; jukujikun readings are FUSED f segments. R6 surfaces the per-point R5 data via `lib/peg.js` (pure, shared by grammar.js + study): `pegHTML()` renders the anime `peg{}` as an attributed quote card **post-explanation / post-answer ONLY** (never a pre-answer cue — it contains the pattern verbatim), `flagBadgesHTML()`/`FLAG_META` render the `flags[]` register chips; the grammar page adds a flag filter (composes with search/sort/◆) + an archetype→register cheat-sheet disclosure (keigo-to-produce vs yakuwarigo-to-recognize).
- `study.js` + `study-lessons.js` + `study-scramble.js` + `study-mcq.js` + `study-duel.js` + `study-gate.js` + `study-build.js` + `lib/study.js` + `lib/questions.js` — the **Grammar Gym** SRS trainer (`#/study`, hidden route): `lib/study.js` is the pure FSRS-lite engine (per-point D/S, stage ladder, mastery gate, queue builder, session helpers, `lessonOrder`/`unitProgress`/`testOutResult`, **R8 `checkpointQuestions`/`recordCheckpoint`/`checkpointPassed`/`nextCheckpointUnit`** — all `now`-parametrised, unit-tested; store shape carries an internal `v` + `UPGRADERS` chain, currently **v3** = v2 adds `settings.placed[]`/`examLevel`, **v3 (R11)** folds `settings.freezes` + `streak.last` into `settings.streak{count,lastDay,freezes,lastFreezeMonth}` and adds `settings.week{done,weekStart}`; the reserved `units:{[unitId]:{checkpoint:{passed,best,attempts}}}` sub-state is written by `recordCheckpoint` without a schema bump); `lib/questions.js` is the pure typed-cloze + answer arbitration + `scrambleFor`/`scramblable` ★-scramble + **R8 `mcqFor(point, allById, exampleIdx, seed?)` 文法形式 4-choice generator** (blanked stem + 4 surfaces = the pattern + 3 curated wrongs from the confusable graph/distractors — the same set `mcqOptions` builds in the validator, replicated locally so runtime never imports the build script — shuffled deterministically by seed). Card modules are shared sub-controllers mounted into the session/lesson/checkpoint shells: `study-scramble.js` (slot-fill), `study-mcq.js` (`mcqCard` — blanked stem + 4 option buttons, recognition-style grade), each auto- or grade-scored. `study.js` is the shell — the **course home** (redesigned to the warm editorial **"climb" home**: the five levels render as a vertical ascent rail to N1 — N5/N4 sealed ✦ vermilion at the base, a "you are here" node on the current level = lowest non-mastered with progress, N2/N1 as the summit above; each rung stays an expand accordion to its unit list from `data/grammar-units.json`; unit glyphs ○/●/✓ then **✓★ gold once its checkpoint passes**; the streak + Σmastered ride the header; ONE priority-ordered ▶ Continue button — **docked in the session card at the current rung** — whose state machine routes **in-flight → placement → due → unlocked checkpoint → lessons → caught-up**; a quiet foot holds 📊 Progress / 🎓 Mock exam / 🎯 Placement + the exam-priority lever + the 🔊 autoplay toggle. The 3-beat lesson (`study-lessons.js`) is likewise editorial — a 1-2-3 Learn→Spot-it→Produce step rail, a serif pattern hero, connection-as-formula chips, a restored non-leak example (never the beat-3 cloze), nuance/register/watch-out as distinct notes, and the anime peg as a pull-quote. Styling is token-driven `.stu-*` under `#studyRoot`; no logic changed in the redesign) plus the session runner (per-answer write-through to `jwh-study-v1`, mid-session resume; format mix rotates cloze/scramble/MCQ by stage, N1/`written-formal` bias to recognition via `gateMode`); `study-lessons.js` is the lazy 3-beat lessons + placement sweep + test-out (2 clozes + 1 confusable MCQ) + **the 10-Q unit checkpoint runner** (≥8/10 → ✓★ + `celebrate()`; below → surface missed + retake — **checkpoints are FORMATIVE, never write scheduling**). `study-duel.js` is the **nuance duel**: a self-contained, formative overlay launched from any grammar card's "vs 〜X" confusable chip — 6 rapid 2-choice discrimination items over one pair; it NEVER calls `review()`/touches the store. `data/grammar-units.json` groups all 353 points into named thematic units (validated by `validateUnits`: every point in exactly one unit, sizes 6–16). **R9 (struggle UX, all over the existing engine logic — never mutating `effectiveGrade`/`review()`):** a **progressive hint ladder** on the cloze card (gloss → structure → kana, each press advancing; the anime peg is NEVER a hint tier — it leaks the pattern) wires the highest tier used into `effectiveGrade({hintTier})` so gloss/structure cap Good, kana caps Hard; **ghost lifecycle UI** (a 👻 haunted badge on a ghost card, a "N haunting" count on the course home, a toast when a ghost clears its 2 clean passes); a **Leech deck** on the course home (`leechList(state)`/`ghostCount(state)` pure selectors) listing leeches (5+ lapses) with pattern/meaning/lapses/confusables + "Duel it" (R8 nuance duel) and "Study" (focused single-card session), suspended leeches surfaced separately with an **Unsuspend** button (`unsuspend(state,id,now)` — clears `suspended`, keeps `leech`, due now; never silently vanishes). The grammar page **reads** `jwh-study-v1` leeches/ghosts and reflects a 👻 marker per point (read-only — no dual-write of scheduling). **R10 (the Mastered gate goes live — `study-gate.js`, surfacing the R1 engine, NOT re-implementing it):** once a point reaches **Deep**, its scheduled reviews run in **gate mode** (`review(…, mode:'gate')`) — `buildCard` overrides the R4/R8 format rotation and hands off to `buildGateCard` (production = hint-free typed cloze — the R9 hint button is suppressed; recognition = MCQ/scramble per `gateMode`), choosing a **not-yet-passed example index first** so a correct answer advances the engine's distinct-example count. Cards show a "🎯 Mastery check — n/3" header + a **soft ~20 s countdown** (`mountGateTimer` — depleting ring, or a static numeric readout under reduce-motion; over-time flags visually but NEVER auto-fails). On the 3rd distinct pass → `celebrate()` + stage `mastered`; a fail → honest "back to Mature, gate restarts" toast (`gateFeedback`). The course home surfaces **mastered count per level** + an **"N in the gate"** count (`masteryStats`, light — full analytics is R15). **`study-build.js` — "Build a sentence" PRACTICE**, offered from the course home for Deep/Mastered points: free textarea → "Show model" (an example sentence, optional TTS via `speak.js`) → DISPLAY-ONLY self-rating (got it/close/missed) — **it NEVER calls `review()`/schedules; the gate is the only mastery truth**. **R11 (habit engine, all pure in `lib/study.js`):** `recordSession(state,todayISO)` — called ONCE from `renderSummary` (guarded so a resumed/re-rendered summary can't re-bump; idempotent per calendar day) — increments the **days-shown-up streak** (next-day +1; a gap resets to 1 unless up to 2 auto-freezes/month bridge the missed day(s); freezes replenish each month via `lastFreezeMonth`) and the **weekly-goal counter** (`settings.week`, resets on the Monday rollover); `streakInfo`/`weeklyInfo`/`masteryStats` (mastered-per-level + `LEVEL_TOTALS`=82/86/72/66/47) are the read-only selectors the dashboard widget consumes. `celebrate()` also fires on a first climb to **Mature** (a real stage-up, not a gate-fail demotion) and on a **level completion** (its final point mastered — stacked after the gate's own Mastered burst), both gated on reduce-motion/celebrations-off. **R13 (timed mock-exam mode — `study-exam.js` + `lib/exam.js`):** a per-level grammar-section simulation launched from the course-home 🎓 Mock exam button (parked in `activeFlow` like the lesson flows). `lib/exam.js` is pure/tested: `buildExam(level, pointsByLevel, passages, seed)` assembles the JLPT composition — N 文法形式 MCQ (`KATA_COUNT` = N5 16 / N4 15 / N3 13 / N2 12 / N1 10) + 5 文の組み立て ★ scramble + 5 文章の文法 passage-blanks — reusing `mcqFor`/`scrambleFor`/the R12 passage bank (never re-authoring), deterministic by seed, with graceful `shortfall` (N2/N1 have no passages until R14 → fewer items, logged, exam still runs); `scoreExam(answers, questions)` → `{raw, total, byFormat, byCluster}` (wrong 形式 picks attributed to the confusable cluster they fell in); `examBand`/`recordExam` (the bounded `settings`-sibling `examLog` ring — reserved-field lazy-init like `units`, NO schema bump — stores `{level,date,raw,total,byFormat}` for R15's trendline). The runner is EXAM-style and deliberately NOT the practice cards: exam numbering (問 n), a question palette (answered/flagged/current) + prev/next nav, a soft ~1 min/item countdown (over-time flags, never cuts off; reduce-motion kills the low-time pulse), **NO per-question feedback and the peg suppressed** (both leak) — feedback lands ONLY on the end report (raw score, indicative band + the HONEST 19/60 sectional-floor label "this mock covers the grammar half only … not a section pass", per-format + per-confusable-cluster breakdown, time-per-question vs budget with the ★ time-sink call-out, per-level readiness hint). Runner classes are `.stu-mock-*` (the `.stu-exam*` prefix is the exam-priority lever). `.stok` tokens NEVER `.jp`. **R14 (data)** authored the N2/N1 文章の文法 passage bank (business/editorial register) + keigo-critical drill coverage through the existing formats — data only, no new module. **R15 (the capstone — analytics, pacing, hardening, the Master moment — `study-stats.js` + pure selectors in `lib/study.js`):** a **📊 Progress** flow (course-home foot button → `startStats(ctx)`, parked in `activeFlow`, lazy `study-stats.js`, every level warmed for patterns + the confusable graph). It renders: **exam-countdown pacing** — `paceProjection(state, now)` projects the honest *all-gates-passed* date (seed-remaining ÷ `settings.newPerDay` + one **engine-derived** maturation lag `masteryLagDays()` — which walks ONE point through on-schedule reviews Seed→Deep→3 gate passes, so the projection tracks the scheduler weights, no magic constant; lands ~Jun 2027 as the plan claims) vs the baked Dec 6 2026 / Jul 4 2027 anchors, plus a **one-tap re-pace** (`repaceNewPerDay(state,'2027-07-04',now)`, clamped 1–40) that writes `settings.newPerDay` **only on explicit tap, never silently**; **course-% rollups** (`courseRollup` — unit → level → overall, **KEYED ON POINT MASTERY / gates, NEVER on checkpoints** — checkpoints can't block 100%; level denominators are the fixed `LEVEL_TOTALS`); **estimated retention today** (`estimatedRetention` — mean FSRS retrievability across started points); per-level **mock trendline** sparklines (`mockTrend` from the `examLog` ring); a **weakest-confusable-cluster rollup** (`clusterWeakness` scores connected-component families of the confusable graph by lapses + leeches + persisted mock `byCluster` misses — `mockClustersFromLog`; the graph → clusters union-find is built in the UI, the scoring is pure); and the **353-cell mastery heat grid** — one `<button>` per point, **dual-encoded** (a 6-step green→gold tint ramp AND a fill-height `<i>` bar AND an aria-label carrying the stage name — never color-only), grouped by level in unit-walk order, **rAF-chunked fill** (64/frame, cancelled on teardown) so 353 cells never block a frame; tapping a cell opens a detail card → **Drill it now** routes back through `ctx.drill` to a focused single-card session (the studyLeech path). The **JLPT Master certificate** (`isMasterComplete(state)` = all 353 at `mastered`; unit-tested 353-true/352-false) is **identity-free** (NO name — five level rings at 100%, `certStats` = lifetime days-shown-up / Σreps reviews / accuracy, completion date), gated so it only shows at 353/353 (or a `localStorage['jwh-study-cert-preview']==='1'` debug preview); the final gate pass auto-opens it via `launchCertificate()` with a `celebrate()` burst (self-gated on reduce-motion). Print uses the classic visibility trick (`body.stu-cert-printing` @media print). **Store shape (v3, `jwh-study-v1`):** `{v, points:{[id]:{D,S,last,due,stage,reps,lapses,ghost,gate,leech,suspended,defers}}, session:{queue,pos,results}|null, units:{[unitId]:{checkpoint}}, log, examLog:[ring of {level,date,raw,total,byFormat,byCluster}], lastSession, settings:{newPerDay,capReviews,weeklyGoal,streak,week,placed[],examLevel,daysStudied}}` — `examLog` + `daysStudied` are additive, reserved-field lazy-init (examLog precedent), NO schema bump. **Data files:** `grammar-n[1-5].json` (the 353 points, R5-enriched: 3 examples + `peg`/`flags[]`/`confusable[]`/`distractors[]`), `grammar-units.json` (~35 named units), `grammar-passages.json` (文章の文法 bank) — all three in SW `ASSETS` + the SWR regex. **Hardening (R15):** every study surface restores focus across `innerHTML` rebuilds, announces via the static `#stuLive` sibling live region, keeps keys scoped to the study root (the R8 `stopPropagation` guard), and rides the global `html[data-reduce-motion="on"]` kill-switch (heat grid, timers, certificate) + `celebrate()`'s own gate; a full session + a mock run offline (every study module + `grammar-*`/`grammar-units`/`grammar-passages` JSON precached); `jwh-study-v1` round-trips through `backup.js` (prefix-walk) including an in-flight `session`. **Audio:** the gym speaks Japanese via `speak.js` — TTS is driven by `lib/grammar.js` `exampleReading(ja)` (concatenates each token's DATA kana reading via `tokenReading`, so homographs like 行った→いった sound right, not the browser's guess), exposed as `speakExample(ja, btn)` + the shared `speakBtnHTML()` 🔊 control. 🔊 buttons appear POST-ANSWER / teaching / reference ONLY (cloze reveal, mcq/scramble feedback, gate reveal via those paths, beat-1 rule card = pattern reading, build model, exam END-report review, grammar reference cards) — NEVER during an unanswered input/pick/place/timed-exam phase (same leak rule as the peg). A persisted **autoplay** opt-in (`KEYS.studyTts` = `jwh-study-tts-v1`, string sentinel `'on'`/`''`, default OFF, `getRaw`/`setRaw`) auto-speaks the example on reveal for the session's cloze/mcq/scramble/gate cards; its 🔊-auto toggle sits in the course-home foot (only when `canSpeak()`). **Sound cues** via `lib/audio.js` `blip` (gated by the EXISTING `jwh-sound` toggle — no new gate): `coin` on a correct answer + test-out pass, `wrong` on a wrong/Again + failed checkpoint; gate-mastered/passed-checkpoint sounds ride `celebrate()`'s own 1-up (no double-blip). Plan: `specs/plans/2026-07-17-grammar-mastery-program.md`.
- `lang.js` (あ EN/日本語 toggle: translates the static **frame** — brand, nav, section/widget/tracker headings, lede intros — from `assets/i18n.js`; researched **card** content stays English. Plus the hover dictionary: GLOSSARY → Jotoba → Jisho), `backup.js` (export/import all device-local data — atomic replace), `tracker.js` (lottery/timed drops), `konami.js` (arcade mode), `dnd.js` (framework-free drag/reorder with keyboard support), `motion.js` (transitions).

A `jwh:data-changed` CustomEvent is dispatched on any user mutation (checklist, due date, calendar, places, day plans) so the dashboard/map/plan re-derive. `jwh:route` fires on navigation; `jwh:cal-quickadd`/`jwh:plan-goto` wire long-press actions into calendar/plan.

### Data model (`tips.json`)
Built by `/tmp/bake.py`-style transform from the research workflow output. Key arrays: `calendar[]` (baked events, color by `category`), `bookByTimeline[]` ("act by X"), `timeSensitive[]` (hard deadlines w/ `dueBy`), `checklist[]` (phased; items carry `id`, optional `requires[]`, `dueBy`), the pillar arrays (`music/geek/building/meetups/restaurants/disney/activities` as content cards with `tier`), `top10[]`, `domains[]` (the searchable findings, with `confidence`), `sources[]`.

**Two card schemas:** searchable "findings" (`domains[].findings[]`: `tip/why/how/impact/confidence`) vs pillar "content cards" (`name/detail/how_or_when/tier/...`). Don't mix them.

## Conventions / constraints

- **Identity-free.** No personal name/employer in content or git authorship (commits use "WHV Guide"). Personalization = which topics appear, in first-person voice.
- **Zero-build, dependency-free.** No bundler/frameworks/new CDNs (Google Fonts only). Must work on plain GitHub Pages with relative paths and `<script type="module">`.
- **Every dynamic string through `esc()`** before `innerHTML`.
- **Calendar events spanning >10 days** render only on their start day in the month grid (see `SPAN_CAP` in `calendar.js`) — otherwise season-long events flood every cell.
- **Notifications fire only on curated deadlines/book-by + due dates the user sets** — not every baked checklist `dueBy` (those would double-count and flood the bell).
- **localStorage keys**: `lib/store.js` `KEYS` is the CANONICAL list (~40 keys — events, spend, anki, grammar, furi, places, day plans, theme…); this doc no longer enumerates them (it drifted). All are `jwh-`-prefixed — backup.js walks the prefix. Bump the `-v1` suffix if a stored shape changes. `store.get()` type-guards against a corrupted/wrong-typed value bricking boot; `getRaw`/`setRaw` string-sentinel keys (furi, reduce-motion…) must NOT be read with `get`. (`jwh-widgetorder-v1` is dead — widgets are static markup.)
- **Service worker:** bump `CACHE` in `docs/sw.js` (see the file for the current version — it moves fast) on any asset change, and add new `assets/*.js` to its `ASSETS` precache list. It's network-first.
- **Accessibility:** prefer real focusable controls (`<button>`) over `role="button"` containers; restore keyboard focus across `innerHTML` rebuilds (calendar/checklist/plan/map sidebar all do); the user-set "Reduce motion" toggle adds `html[data-reduce-motion="on"]` which kills animations.
- **Calendar data flow is single-path:** user mutations call `saveUser()` → dispatches `jwh:data-changed` → `calendar.render()` + `dashboard.refresh()` both listen. Do NOT also call `render()` at the mutation site (causes a double render). `render()` must never dispatch `jwh:data-changed` (infinite loop). The calendar has a sticky legend-as-filter, a side deadline panel (month mode only), and a day-click popover whose listeners are cleaned up in `dismissPopover()`.
- **Data confidence:** every researched item carries `confidence` (high/medium/low). 2026–2027 dates are estimates flagged medium/low with "verify closer" — don't present them as certain.
