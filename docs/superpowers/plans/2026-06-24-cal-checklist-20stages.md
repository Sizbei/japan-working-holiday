# Calendar (Notion) + Checklist (Todoist) — 20-Stage Build

> **Autonomous loop state.** Each wake: read this file, pick the FIRST unchecked stage whose deps are met, execute it, run its verify, check it off + commit, then continue or re-schedule. Branch: `feat/cal-checklist-redesign`. Baseline: 160 tests. ultracode = use workflows for parallel-safe stages; serialize shared-file edits (content.js / calendar.js / style.css). Self-prompt cadence ~30 min.

## Locked design decisions (from two adversarial-review rounds)
- **Checklist** (high-confidence): extract out of content.js; smart views Today/Upcoming/Overdue; p1–p4 priorities. **Priority is UI-only — it NEVER bypasses `requires[]` dependency locks.** `checklistItems()` stays an importable pure export (dashboard.js + readiness depend on it). p1–p4 needs a `jwh-check-priority-v2` map + migration (v1 array→p1).
- **Calendar week view** (hardened): **drag-reschedule on SINGLE-DAY events ONLY** (multi-day seasonal bars are click-to-edit, never draggable — dragging a 5-month bar would shift the whole window). Use the app's **real `--c-*` category colors** (festival #c43459, music #5a3fb5, seasonal #8a6508, … all defined in style.css:33-45 with `-ink` dark variants). **Today = red outline** (`box-shadow: inset 0 0 0 1px var(--red)`), NOT a filled circle. **Lane-pack** multi-day bars (greedy), don't give each its own full-width row. Per-day always-visible **"+" add** affordance. Mobile = **vertical 7-day list** (reuse agenda rendering bounded to the week; no horizontal grid → no route-swipe conflict).
- **CUT** (do not build): hourly time-grid, timed events, sub-tasks, recurring, NL quick-add.
- Conventions: zero-build ES modules; `esc()` every dynamic string (escapes `& < > "`, double-quote attrs); single-dispatch `jwh:data-changed` (no render() at mutation site, render() never dispatches); versioned KEYS; bump SW `CACHE` + precache new `assets/*.js`; pure libs unit-tested (node --test).

---

## PHASE A — Checklist

- [x] **Stage 1 — Pure libs (parallel-safe; ultracode workflow).** ✓ priority.js (14) + smartviews.js (12) + weekgrid.js (20) = 46 tests, 206 total green, import-safe. Create `lib/priority.js` (p1–p4: `migratePriority(v1array)→map`, `getLevel(map,id)`, `setLevel(map,id,level)` returns new map, `priorityRank(level)`, `byPriority` comparator) + `tests/priority.test.mjs`; `lib/smartviews.js` (`classifyDue(effectiveDue, todayISO)→'overdue'|'today'|'upcoming'|'none'`, `filterView(items, view, todayISO)`, `groupByDay(items)`) + `tests/smartviews.test.mjs`; `lib/weekgrid.js` (`weekStart(iso,weekStartsOn=0)`, `weekDays(iso)→7 ISO`, `clampSpan(ev,weekStart,weekEnd)→{col0,col1,contL,contR}|null`, `isMultiDay(ev)`, `packLanes(multiDayEvents)→[{ev,lane,col0,col1,contL,contR}]` greedy) + `tests/weekgrid.test.mjs`. All pure, import-safe, TDD. Verify: `node --test tests/*.mjs` green (+~20). **No commits inside the workflow — the loop commits the batch.**
- [x] **Stage 2 — Extract checklist from content.js.** ✓ content.js 857→369; new checklist-page.js (513); dashboard repointed; SW v92; 206 tests green; browser-verified (checklist 74 items + progress + dashboard readiness; 0 console errors). content.js is 857 lines (over 800). Move the checklist render/wire (renderChecklist/renderCheckTools/renderCheckToolbar/wireChecklist/openCheckEditor/etc., ~371-620+) into a new `checklist-page.js`; move pure `checklistItems`/`orderItems` into `lib/checklist.js` (KEEP exported — dashboard.js:10 + readiness import them). content.js imports + calls them from `renderContent`. The `jwh:data-changed` dispatch stays at mutation sites. Verify: node tests green; browser-verify checklist renders + completes + dashboard readiness/teasers still work; 0 console errors. SW bump + precache `checklist-page.js`. Commit.
- [x] **Stage 3 — p1–p4 priority model wiring.** ✓ KEYS.checkPriorityV2; loadPriority migrates v1 array→{id:1} lazily; all readers level-aware via lib/priority. Browser-verified migration→P1. Use `lib/priority.js`. Add `KEYS.checkPriorityV2='jwh-check-priority-v2'`; migrate v1→v2 on load (existing flagged → p1). Update all readers (was `Set.has`) to level-aware. Verify: node tests green. Commit.
- [x] **Stage 4 — p1–p4 priority UI.** ✓ ⚑ cycles none→P1→P2→P3→P4→none; colored dots (p1 red/p2 rust/p3 indigo/p4 grey); aria-label per level; due-soon sorts by priorityRank. Locks inviolable (verified). SW v93. 0 console errors. Replace the binary ⚑ with a 4-level picker (click cycles p1→p2→p3→p4→none, or a small popover): p1 red / p2 orange / p3 blue / p4 grey dot; `aria-label` reflects level. Sort within a view by priority then due. **Confirm `requires[]` lock logic is untouched** (priority never unlocks). SW bump, browser-verify. Commit.
- [x] **Stage 5 — Smart-view pills.** ✓ All/Today/Upcoming/Overdue replace the binary toggle (KEYS.checkSmartView); Today=due-today+overdue, Upcoming=next 7d grouped by day, Overdue=<today; All keeps phase grouping; per-view empty states; persisted. Browser-verified all 4 (overdue⊂today, upcoming day-headers). SW v94. 0 console errors. Add segmented pills **All · Today · Upcoming · Overdue** above the list (persist selection in a new `jwh-check-smartview` raw key). Use `lib/smartviews.js`. **Today** = due today + overdue; **Upcoming** = next 7 days grouped by day (date subheaders); **Overdue** = effectiveDue < today. Keep the existing phase grouping for "All". Empty states per view. SW bump, browser-verify. Commit.
- [x] **Stage 6 — Checklist adversarial review + fixes (Phase A done).** ✓ 3 critics; verified + fixed: focus-restore on pills/hide-done, P2 dark contrast (--c-food-ink), level numeral (non-color cue), aria-pressed sync during search, locked items excluded from smart views, migration-persist-once, dead export removed, it.id guard. Rejected: Today-incl-overdue relabel (Todoist convention), roving-tabindex (app-consistent). SW v95, 206 tests, 0 console errors. Run an adversarial-review (subagents, 2–3 lenses: correctness/migration, a11y/UX, regression vs dependency-locks + dashboard). Fix confirmed findings. Full regression: node tests green, browser smoke, 0 console errors. **Merge Phase A to main? No — keep on the branch; mark done.**

## PHASE B — Calendar

- [x] **Stage 7 — Revise the mock.** ✓ Real --c-* tokens (color-mix tints), today=red outline, per-day ＋ row, weekend Sat+Sun only (Fri fixed), teamLab→illumination. Design-critic review: fixed mobile .mev illum (Planets+Borderless), legend illumination entry, ink to match app (#1a1a22/#595550). Sent to user. Update `docs/mockups/calendar-week.html`: swap invented palette for real `--c-*` tokens; today = red outline not filled circle; lane-pack bars (admin+teamLab share a lane where they don't overlap); add a per-day "+" affordance; fix the Friday `.weekend` bug (only Sun+Sat). Serve + screenshot. Commit.
- [x] **Stage 8 — `lib/weekgrid.js`** ✓ built in Stage 1 (20 tests).
- [x] **Stage 9 — calendar.js week mode skeleton.** ✓ mode=week + Month/Week/Agenda toggle (#calModeWeek); renderWeek/weekHTML 7-col header (outline-today, weekend Sat+Sun), weekLabel range; prev/next + Today step by week; mode-aware prev/next aria. lib/weekgrid weekDays. Browser-verified toggle/label/headers/today/shift/back-to-month. SW v96, 206 tests, 0 console errors. Proportionate self-review (full review at Stage 15/18). Add `mode='week'` + a Month/Week/Agenda toggle (`#calModeWeek`); `renderWeek()` builds the 7-col header (real outline-today, weekend tint Sat+Sun only), wired to viewY/viewM/a week anchor. `[`/`]` + prev/next shift by a week in week mode. Verify: toggling shows the header. Commit.
- [x] **Stage 10 — week all-day lane.** ✓ packLanes multi-day BARS (grid-column span, --cat:var(--c-<cat>) + color-mix tint, ‹ › continue arrows) + single-day CHIPS per column; allEvents()+visible() (no SPAN_CAP); esc all titles. Critic review: added safeCat() whitelist fallback (unknown .ics category → --c-imported, verified). SW v97, 206 tests, 0 console errors. Browser-verified bars span, colors category-driven, chips render. Render packed multi-day bars (`lib/weekgrid.packLanes`) with real category colors (`var(--c-<cat>)` tint + ink) + continue `‹ ›` arrows; single-day events as chips per column. Use `eventsOn(date,false)` (NOT the month SPAN_CAP). esc() all titles. Commit.
- [ ] **Stage 11 — per-day "+" quick-create.** An always-visible small "+" per day column (even when bars cover it) → dispatches `jwh:cal-quickadd {date}` → existing `openModal(null,date)`. Commit.
- [ ] **Stage 12 — week drag (single-day only) + click-edit.** Make ONLY single-day events draggable (reuse `makeMovable` + override/`shiftEnd`); multi-day bars get `cursor:default` + click→edit modal/popover. Confirm baked vs user both reschedule via the override path. Browser-verify a single-day drag moves it; a multi-day bar does NOT drag. Commit.
- [ ] **Stage 13 — mobile week = vertical 7-day list.** At ≤700px, `renderWeek` outputs a vertical day-by-day list (reuse/adapt the agenda renderer bounded to the 7 week days; multi-day events flagged ongoing). No horizontal grid. Commit.
- [ ] **Stage 14 — week-grid CSS.** Desktop grid + bars + chips + per-day "+" + mobile list, all on real tokens; reduce-motion aware. Commit.
- [ ] **Stage 15 — SW bump + browser-verify week view.** Desktop (colors correct, today outline, bars packed, single-day drag works, multi-day not draggable, per-day add works) + mobile (vertical list). 0 console errors. Commit.
- [ ] **Stage 16 — month-view continuous bars.** Improve the MONTH grid: render multi-day events as continuous bars across cells (vs repeated chips), reusing the span logic; keep SPAN_CAP flood-guard sane. Commit.
- [ ] **Stage 17 — browser-verify month bars.** Multi-day events read as continuous; long seasonal events still don't flood every cell. 0 console errors. Commit.
- [ ] **Stage 18 — Calendar adversarial review.** Subagents, lenses: feasibility/override-correctness, design-taste vs the app, UX (drag-single-day-only honored, click-targets, mobile). Fix confirmed findings. Commit.
- [ ] **Stage 19 — full regression.** `node --test tests/*.mjs` green; smoke all 13 routes (no overflow, render, 0 console errors); `python3 -m json.tool docs/data/tips.json`. Fix anything. Commit.
- [ ] **Stage 20 — finish.** Final whole-branch code review (subagent). Merge `feat/cal-checklist-redesign` → main, push (deploys Pages). Report. END the loop (no further self-prompt).

## Loop bookkeeping (updated 2026-06-24)
- **Cadence: ~600s (10 min).** Re-schedule with `ScheduleWakeup(~600s)` each cycle.
- **Every stage gets the full goal cycle: (1) brief plan, (2) build, (3) adversarial/AI review (2–3 blind subagent critics on the stage's diff; verify findings yourself, reject wrong ones), (4) fix confirmed findings, (5) verify (node --test + browser where UI), (6) commit + check the box.** ultracode: run the per-stage review as a small workflow/parallel critics; do the build in a workflow when parallel-safe, else the main loop.
- If subagents/workflows hit a session/rate limit: do the stage + a lighter self-review in the main loop, or back off and re-schedule.
- Don't merge to main until Stage 20 (keep everything on the branch).
